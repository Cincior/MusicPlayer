<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MusicPlayer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
    <style name="Theme.SplashScreen" parent="Theme.MusicPlayer">
        <item name="android:windowSplashScreenBackground">@color/skyBlue</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.MusicPlayer</item>
    </style>
</resources>